/**
 * Re-exports the shared PlacementRanker logo from `@digimine/ui`. The admin
 * portal used to have its own copy with the old gem mark — that's now gone,
 * so the brand stays in sync with the public site.
 */
export { Logo, type LogoProps } from "@digimine/ui";
