<?xml version="1.0" encoding="UTF-8"?>
<!--
  Hardened-runtime entitlements for the macOS build (referenced from
  package.json → build.mac.entitlements). These let the OS grant the agent the
  capabilities it needs once the user approves them in System Settings:

    - audio-input / camera      : optional cam+mic share in a session.
    - JIT / unsigned-executable : Electron/Chromium runtime requirements.

  Screen Recording and Accessibility (the remote-input permission) are NOT
  entitlements — macOS gates them at runtime via TCC, and the user must enable
  them under System Settings → Privacy & Security (see README). The
  NS*UsageDescription strings live in package.json → build.mac.extendInfo.
-->
<plist version="1.0">
  <dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <key>com.apple.security.device.camera</key>
    <true/>
  </dict>
</plist>
